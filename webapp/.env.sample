# Sample .env file for AnonyConnect Webapp
# Copy this file to .env and fill in your values

# Node environment
NODE_ENV=production

# Admin username for login
ADMIN_USERNAME=admin

# Admin password (set a strong password in production)
ADMIN_PASSWORD=changeme

# Admin contact email (used as VAPID contact for Web Push and administrative notices)
# Recommended: a role address such as admin@yourdomain.com or abuse@yourdomain.com.
# This value is used only as the `mailto:` contact passed to push services (see VAPID settings);
# it is not used to send emails to ticket submitters automatically. If unset, the code falls
# back to `admin@example.com`.
ADMIN_EMAIL=admin@example.com

# Session secret for cookies (use a long random string)
SESSION_SECRET=your_session_secret_here

# Port for Next.js (default: 3000)
PORT=3000

# Example: Telegram bot token (if used)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Cloudflare Turnstile site key (if used)
CF_TURNSTILE_SITE_KEY=your_site_key
CF_TURNSTILE_SECRET_KEY=your_secret_key

# Web Push (VAPID) keys for browser notifications (optional)
# Generate keys locally with: `npx web-push generate-vapid-keys --json`
# Then set the public/private values below. The public key is exposed to the client;
# the private key must remain secret on the server.
# Example:
# VAPID_PUBLIC=BPu...your_public_key_here...
# VAPID_PRIVATE=K8...your_private_key_here...
# Also expose the public key to the client as NEXT_PUBLIC_VAPID_PUBLIC_KEY
# e.g.: NEXT_PUBLIC_VAPID_PUBLIC_KEY=$VAPID_PUBLIC
VAPID_PUBLIC=
VAPID_PRIVATE=
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
